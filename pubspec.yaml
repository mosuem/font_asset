name: font_asset
description: A class for font assets to be used in Flutter for the hook system.
version: 0.0.7
repository: https://github.com/mosuem/font_asset

environment:
  sdk: ^3.10.0

dependencies:
  code_assets: ^1.0.0
  data_assets: ^0.19.6
  flutter_hook_config: ^0.0.2
  hooks: ^1.0.0
  mime: ^2.0.0
  native_toolchain_c: ^0.17.4
  path: ^1.9.1

dev_dependencies:
  dart_flutter_team_lints: ^3.5.2
  lints: ^6.0.0
  test: ^1.28.0
