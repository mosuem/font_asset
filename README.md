### package:font_asset

A class for font assets to be used in Flutter for the hook system. Enables to add fonts via the build hook instead of through the pubspec yaml, and thus to do stuff like icon tree shaking independently of Flutter.